<% if @patients.blank? %>
	<h1>No Patients to display </h1>
<% else %>
	<ul>Patients List 
		<% @patients.each do |patient|%>
			<li><%= link_to patient.email, blood_glucose_level_path(patient_id: patient.id), method: :get %></li>
		<% end %>
	</ul>
<% end %>